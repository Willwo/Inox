#require 'rubygems'
require 'spec/rake/spectask'

Spec::Rake::SpecTask.new('spec/core') do |t|
  t.spec_files = FileList['spec/core/*_spec.rb']
  # t.rcov = true
  # d = File.expand_path('core/')
  # t.ruby_opts << "-I\"#{d}\""
end